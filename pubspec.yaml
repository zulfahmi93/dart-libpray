name: libpray
description: Muslim prayer times library for Dart which includes various calculation and juristic method that can be customised.
version: 0.1.0+2
author: Zulfahmi Ahmad <zulfahmi93@outlook.com>
homepage: https://github.com/zulfahmi93/dart_libpray

environment:
  sdk: '>=2.0.0-dev.52.0 <3.0.0'

dependencies:
  libcalendar: ^0.1.0
  logging: ^0.11.3+1
  meta: ^1.1.5
  built_collection: ^3.1.3
  built_value: ^5.5.5

dev_dependencies:
  test: ^1.3.0
  mockito: ^3.0.0
  build_runner: ^0.10.0
  built_value_generator: ^6.0.0
